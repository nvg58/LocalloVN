<?xml version="1.0" encoding="utf-8"?>
<com.locol.locol.viewa.CheckableRelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/category_item"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/category_item_bg"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/image"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_gravity="center"
        android:padding="@dimen/size_word"
        android:src="@mipmap/ic_explore_grey600_24dp" />

    <TextView
        android:id="@+id/text"
        android:layout_below="@id/image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="@dimen/size_half_byte"
        android:text="Category"
        android:textColor="@color/color_primary_text"
        android:textSize="@dimen/abc_text_size_medium_material" />

    <TextView
        android:visibility="invisible"
        android:id="@+id/hidden_save_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</com.locol.locol.viewa.CheckableRelativeLayout>